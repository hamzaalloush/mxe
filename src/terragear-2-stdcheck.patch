This file is part of MXE.
See index.html for further information.

Contains ad hoc patches for cross building.

From 5a2a63477c314ee75fdeffaceaae8346a061115d Mon Sep 17 00:00:00 2001
From: MXE
Date: Fri, 5 Jun 2015 18:57:16 +0300
Subject: [PATCH] terragear: reliable check for std::isnan and std::isfinite


diff --git a/CMakeLists.txt b/CMakeLists.txt
index d95f2b1..63d5e10 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -123,6 +123,22 @@ if(CMAKE_COMPILER_IS_GNUCXX)
     set(WARNING_FLAGS -Wall)
 endif(CMAKE_COMPILER_IS_GNUCXX)
 
+##Support for finding STD_ISNAN on MXE/MINGW
+if(NOT HAVE_STD_ISNAN)
+check_cxx_source_compiles(
+  "#include <cmath>
+  int main() { double x=0.1; return std::isnan(x)?1:0; }"
+  HAVE_STD_ISNAN)
+endif()
+
+##Support for finding STD_ISFINITE on MXE/MINGW
+if(NOT HAVE_STD_ISFINITE)
+check_cxx_source_compiles(
+  "#include <cmath>
+  int main() { double x=0.1; return std::isfinite(x)?1:0; }"
+  HAVE_STD_ISFINITE)
+endif()
+
 if(WIN32)
 
     if(MSVC)
-- 
1.9.1

